## 1. 项目骨架与 Harness 集成

- [x] 1.1 建立 RepoAtlas 插件、配置和测试目录结构，并确认只依赖 DeepSeek Harness 的公开插件接口。
- [x] 1.2 定义 v1 配置模型：workspace 根目录、默认排除路径、敏感文件模式、文件与 ReAct 资源预算。
- [x] 1.3 注册 RepoAtlas 的 GoalSpec、Policy Gate、只读工具、证据记录和报告服务，并验证插件可以在 Harness Web UI session 中加载。 <!-- PASS 2026-08-14: `cordis.patch.yml` 已作为 bundle 接入临时 Web profile；`DSH_HOME=/private/tmp/repoatlas-dsh-home pnpm dsh --profile web --dump-config` 显示 `repo-atlas -> repo-atlas/harness`，真实 Web UI `http://127.0.0.1:51748` 的“设置 → 插件 → 插件列表”显示 `repo-atlas/harness` 为“已挂载、已启用”。 -->

## 2. 多轮目标澄清

- [x] 2.1 定义并验证 GoalSpec 结构，覆盖 `intent`、`audience`、`scope`、`outputs`、`permissions`、`success_criteria` 和 `confirmed`。
- [x] 2.2 实现缺失字段检测和逐轮问题选择，确保每轮最多提出一个主要问题，并提供安全默认值。
- [x] 2.3 实现目标复述、用户确认和“直接开始”安全默认流程；未确认目标不得进入深度分析。
- [x] 2.4 实现分析后的追问、范围收窄和 GoalSpec 更新，确保已有 session 证据可以复用。

## 3. v1 安全边界

- [x] 3.1 实现路径规范化、workspace containment 检查和 workspace 外符号链接拒绝。
- [x] 3.2 实现敏感路径拒读、Secret-like 内容脱敏和脱敏证据记录，覆盖 `.env`、私钥、凭证和常见密钥模式。
- [x] 3.3 实现默认拒绝的 Policy Gate，拒绝写入、删除、任意 Shell、网络、依赖安装、Git 推送和外部服务调用。
- [x] 3.4 实现仓库内容不可信策略，确保 README、注释、脚本和配置中的指令不能改变系统权限或工具授权。
- [x] 3.5 实现文件数量、单文件大小、总读取量和 ReAct 动作数限制，并记录拒绝、跳过、预算耗尽和失败原因。

## 4. 只读代码库分析

- [x] 4.1 实现 workspace 文件列举、排除目录过滤、普通文本读取、文本搜索和常见配置解析工具。
- [x] 4.2 实现项目接手概览分析计划，产出技术栈、入口、核心目录、运行配置和推荐阅读顺序所需的观察。
- [x] 4.3 实现架构概览分析计划，产出模块关系、依赖方向、主要配置和静态边界所需的观察。
- [x] 4.4 实现受 Policy Gate 约束的 ReAct 只读循环，记录每个动作、观察、停止原因和关联证据。
- [x] 4.5 实现解析失败、不可读文件、二进制文件、超限文件和用户中断时的部分结果保留。

## 5. 证据化代码星图报告

- [x] 5.1 定义证据记录模型，至少保存证据 ID、来源路径、定位信息、观察内容、状态和脱敏状态。
- [x] 5.2 实现主要结论到证据的关联校验，阻止无证据结论以确定语气进入报告。
- [x] 5.3 实现 Markdown 报告，包含项目摘要、技术栈、目录结构、核心模块、入口、依赖、测试配置和阅读顺序。
- [x] 5.4 实现 Mermaid 关系图和结构化 atlas 数据输出，标记关系来源及未确认部分。
- [x] 5.5 实现已确认、推测、未分析、读取失败、安全跳过和预算耗尽等状态的统一展示。
- [x] 5.6 实现当前 session 内的证据感知追问，并确保新增探索只覆盖追问相关范围。
- [x] 5.7 实现报告导出确认流程；未经用户确认不得写入用户代码库或其他目标路径。

## 6. 验收测试与安全测试

- [x] 6.1 准备覆盖项目接手概览、架构概览、目录范围和部分失败的本地 fixture 仓库。
- [x] 6.2 增加多轮澄清、GoalSpec 更新、确认和追问复用的行为测试。
- [x] 6.3 增加路径遍历、符号链接逃逸、敏感文件、Secret-like 值和脱敏日志测试。
- [x] 6.4 增加写入、Shell、网络、依赖安装、Git 推送和第三方指令注入的拒绝测试。
- [x] 6.5 增加大文件、二进制、超出扫描预算、解析失败和用户中断的边界测试。
- [x] 6.6 运行项目类型检查、静态检查、单元测试、OpenSpec validate 和 Harness 插件加载验证，并记录 PASS/FAIL/BLOCKED 结果。 <!-- PASS 2026-08-14: `npm run typecheck`、`npm test`（16/16）、`npm run lint`、`npm run validate:openspec` 与真实 Harness Web UI bundle/plugin 加载均 PASS。 -->

## 7. 使用文档与 v1 交付

- [x] 7.1 编写 RepoAtlas v1 使用说明，明确启动方式、目标澄清流程、两个分析模板和报告内容。
- [x] 7.2 编写安全边界说明，明确默认只读、无网络、无任意命令和敏感文件策略。
- [x] 7.3 编写限制和后续路线说明，明确受控命令执行、增量地图和代码交付不属于 v1。
- [x] 7.4 在至少一个真实本地代码库上完成端到端演示，并根据失败案例更新配置、报告和任务验收说明。
